# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @backend/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t @backend/auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system @backend/auth-service && \
          adduser --system -G @backend/auth-service @backend/auth-service

COPY apps/auth-service/dist @backend/auth-service/
RUN chown -R @backend/auth-service:@backend/auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix @backend/auth-service --omit=dev -f install

CMD [ "node", "@backend/auth-service" ]
